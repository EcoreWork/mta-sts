include:
     - .well-known
```
5. Click **Commit changes** → **Commit changes**

---

### Step 5 — Enable GitHub Pages

1. Go to repo **Settings** (tab at the top)
2. In the left sidebar, click **Pages**
3. Under **Source**, select:
   - **Branch**: `main`
   - **Folder**: `/ (root)`
4. Click **Save**
5. Wait 1-2 minutes for deployment

---

### Step 6 — Set Custom Domain

Still in **Settings** → **Pages**:

1. Under **Custom domain**, enter:
```
   mta-sts.yourdomain.com
```
2. Click **Save**
3. ✅ Check **Enforce HTTPS** (may take a few minutes to become available)

---

### Step 7 — Add DNS Record in BigRock/HostGator

Go to your DNS management and add a **CNAME record**:

| Name | Type | Value |
|------|------|-------|
| `mta-sts` | CNAME | `yourusername.github.io` |

> Replace `yourusername` with your actual GitHub username

**Example:** If your GitHub username is `santhosh123`:

| Name | Type | Value |
|------|------|-------|
| `mta-sts` | CNAME | `santhosh123.github.io` |

---

### Step 8 — Wait & Verify

1. Wait **30 minutes to a few hours** for DNS propagation
2. GitHub will automatically issue an SSL certificate
3. Test by visiting:
```
   https://mta-sts.yourdomain.com/.well-known/mta-sts.txt
```
4. You should see the policy text in plain text over HTTPS

---

### Your Final Repo Structure Should Look Like:
```
mta-sts/
├── _config.yml
├── README.md
└── .well-known/
    └── mta-sts.txt
